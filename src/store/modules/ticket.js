const state = {
    showTicketForm: false,
    ticket: {
    }
  }
  
  const getters = {
  }
  
  const actions = {
  }

  const mutations = {
  }
  
  export default {
    namespaced : true,
    strict: process.env.NODE_ENV !== 'production',
    state,
    getters,
    actions,
    mutations
  }
  