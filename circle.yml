machine:
  node:
    version: 6.9.1

dependencies:
  override:
    - npm install 
test:
  override:
  - npm run unit
  - CODECLIMATE_REPO_TOKEN=$CodeClimateToken ./node_modules/.bin/codeclimate-test-reporter < test/unit/coverage/lcov.info
